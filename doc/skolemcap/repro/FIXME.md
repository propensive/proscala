# FIXME: no minimal reproduction known

The [feature doc](../skolemcap.md) states that no self-contained reproduction
is known — the behaviour depends on symbol-completion order and can flip
between zinc and batch `dotc`; the regression test is the Soundness build
itself. If a self-contained reproduction is ever found, it belongs here.
