# FIXME: no minimal reproduction known

The [feature doc](../boundscap.md) states that a minimal file pair does *not*
reproduce the AssertionError — the crash depends on full module context, and
the regression test is the Soundness build itself. If a self-contained
reproduction is ever found, it belongs here.
