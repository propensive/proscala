# FIXME: no standalone reproduction yet

The [feature doc](../inertcache.md) shows only fragments (large unions of
type aliases) of the shape that made capture-checking Setup's alias
re-traversal exponential. A self-contained file that demonstrably hangs (or
measurably blows up) an unpatched compiler still needs to be distilled and
verified.
