# FIXME: no standalone reproduction yet

The [feature doc](../staleread.md) gives only a prose scenario (TASTy-loaded
collections plus a not-yet-compiled macro; the `Loadable` snippet there is
illustrative and does not trigger the crash by itself). A self-contained
multi-file reproduction of the `staleSymbolError` crash still needs to be
distilled and verified.
