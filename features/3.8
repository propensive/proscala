# Patches carried by the 3.8 stream — the <patch> part of feature/3.8/<patch>.
# Authoritative: bin/proscala-rebase-tree rebuilds trunk/3.8 from exactly these.
# See AGENTS.md ("The stream feature lists"). Order is irrelevant; blank lines
# and #-comments are ignored. Do not list `make` (the build lives on `main`).

aliascap
boundscap
castbox
ctxresult
dictcaps
iarraypure
iarraypure-mutalias
inlineupdate
sambox
semdiag
skolemcap
splicealias
spreadable
unboxedpure
wasm
wasm-witcall
