# Patches carried by the 3.9 stream — the <patch> part of feature/3.9/<patch>.
# Authoritative: bin/proscala-rebase-tree rebuilds trunk/3.9 from exactly these.
# See AGENTS.md ("The stream feature lists"). Order is irrelevant; blank lines
# and #-comments are ignored. Do not list `make` (the build lives on `main`).

aliascap
blockimport
castbox
ctxresult
dependarg
depset
dictcaps
givencache
givenprefix
iarraypure
iarraypure-mutalias
inertcache
inlineupdate
lazycycle
literate
nullreceiver
permitlazy
proxyskolem
retainbounds
returnavoid
sambox
samstateful
searchdiag
semdiag
skolemcap
smap
splicealias
spreadable
staleread
unboxedpure
virtualdir
wasm
wasm-witcall
